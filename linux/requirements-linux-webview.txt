# Linux-specific requirements for webview support
# Install these with: pip install -r requirements-linux-webview.txt

# Core webview library
pywebview>=4.4.0

# Qt backend (preferred for KDE desktop environments)
PyQt5>=5.15.0
PyQt5-tools>=5.15.0
PyQtWebEngine>=5.15.0

# Alternative Qt backend (newer, also works well)
# Uncomment these lines if you prefer PySide6 over PyQt5
# PySide6>=6.5.0
# PySide6-WebEngine>=6.5.0

# GTK backend removed - focusing on Qt5 for KDE desktop environments
# This build is optimized for KDE/Plasma desktop with Qt5 webview support

# Required for PyInstaller builds with Qt support
pyinstaller>=6.0.0

# Additional Qt/GUI dependencies that might be needed
# These are often bundled but may need explicit inclusion
typing-extensions>=4.0.0